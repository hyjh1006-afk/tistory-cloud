
## 2026-09-04
- [시더빌 리마스터] 4년 전 번역(9편, /37~/45)을 새로 번역해 같은 카테고리에 재연재. 제목 뒤에 `(리마스터)`,
  5편 제목은 오역 수정(`The Sawbone` "뼈 썰이"→"뼈톱"). 예약 발행 9/5~9/13 매일 21:00 (/154,159~166).
  원본 포맷 유지: `원출처` 링크 + "원작자에게 허락받은 번역본입니다." + 해체 반말.
- [도구] `scripts/cedar_build.py`(번역 텍스트→티스토리 HTML), `scripts/cedar.sh`, `scripts/cedar_post.sh`(예약 발행 자동화)
- [함정] 레딧은 이 IP를 403으로 막는다 — 원문은 브라우저 탭에서 `fetch("/comments/<id>.json")` 로 받아
  `aside.sh eval ... > 파일` 로 저장하면 잘림 없이 들어온다.
- [함정] 티스토리 카테고리 인덱스는 사용자가 카테고리를 늘리면 밀린다. **이름으로 찾아 클릭**할 것.
  (이날 두 줄=11, 단편=12, 시더빌=7로 바뀌어 있었다. 매일 발행 루틴도 이름 방식으로 고침)
- [함정] 같은 주소의 **좀비 탭**이 살아있는 탭을 가려서 CDP 명령이 죽은 탭으로 간다 → 전부 타임아웃.
  `aside.sh close <조각>` 로 닫고 다시 열면 된다. eval 안에서 `location.href` 이동 금지(대화상자로 렌더러가 언다), `goto` 사용.
- [운영] 티스토리는 블로그당 임시저장 슬롯이 하나라 **두 세션이 동시에 편집기를 열면 서로 덮어쓴다.**
  이날 여행 글 세션과 충돌해 본문이 통째로 날아갔다. 작업 전 다른 세션과 순서를 맞출 것.
