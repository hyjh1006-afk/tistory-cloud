
## 2026-09-04
- [시더빌 리마스터] 4년 전 번역(9편, /37~/45)을 새로 번역해 같은 카테고리에 재연재. 제목 뒤에 `(리마스터)`,
  5편 제목은 오역 수정(`The Sawbone` "뼈 썰이"→"뼈톱"). 예약 발행 9/5~9/13 매일 21:00 (/154,159~166).
  원본 포맷 유지: `원출처` 링크 + "원작자에게 허락받은 번역본입니다." + 해체 반말.
- [도구] `scripts/cedar_build.py`(번역 텍스트→티스토리 HTML), `scripts/cedar.sh`, `scripts/cedar_post.sh`(예약 발행 자동화)
- [함정] 레딧은 이 IP를 403으로 막는다 — 원문은 브라우저 탭에서 `fetch("/comments/<id>.json")` 로 받아
  `aside.sh eval ... > 파일` 로 저장하면 잘림 없이 들어온다.
- [함정] 티스토리 카테고리 인덱스는 사용자가 카테고리를 늘리면 밀린다. **이름으로 찾아 클릭**할 것.
  (이날 두 줄=11, 단편=12, 시더빌=7로 바뀌어 있었다. 매일 발행 루틴도 이름 방식으로 고침)
- [함정] 같은 주소의 **좀비 탭**이 살아있는 탭을 가려서 CDP 명령이 죽은 탭으로 간다 → 전부 타임아웃.
  `aside.sh close <조각>` 로 닫고 다시 열면 된다. eval 안에서 `location.href` 이동 금지(대화상자로 렌더러가 언다), `goto` 사용.
- [운영] 티스토리는 블로그당 임시저장 슬롯이 하나라 **두 세션이 동시에 편집기를 열면 서로 덮어쓴다.**
  이날 여행 글 세션과 충돌해 본문이 통째로 날아갔다. 작업 전 다른 세션과 순서를 맞출 것.

## 2026-09-04
- [함정] 이어받은 글쓰기 탭에 **남의 임시저장 초안이 복원돼 있으면** 제목칸이 안 비어 있다 —
  paste 로 본문만 갈아끼우면 제목이 `이전제목+새제목` 으로 붙고 URL 슬러그도 옛 제목으로 남는다.
  `type` 은 insertText 라 선택영역을 대체하므로, 넣기 전에 `setSelectionRange(0,value.length)` 로 전체 선택할 것.
- [함정] 그 상태에서 `완료` 를 누르면 발행 레이어가 **"저장중" 으로 잠긴 채 안 풀린다**(라디오·버튼 전부 disabled).
  좌표 클릭도 안 먹는다. 저장은 안 된 상태이니 탭을 닫고 `/manage/newpost/` 를 새로 열어 처음부터 다시 하면 정상.
  정상일 때는 레이어를 연 직후 버튼이 **"공개 발행"** 으로 보인다 — "저장중" 이면 잠긴 것.
- [함정] `aside.sh` 탭 매칭은 URL **부분일치**라 `tistory.com` 같은 조각은 새로 뜬 `/manage/posts/` 탭을 잡는다.
  글쓰기 탭은 반드시 정규식으로: `'~/manage/newpost/$'`
- [함정] `/manage/posts/` 목록은 클라이언트 렌더링이라 `fetch` 하면 껍데기(16KB)만 온다. DOM 을 읽거나,
  비공개 확인은 `?visibility=hidden` 필터로 들어가면 행에 글번호 링크까지 같이 나온다.

## 2026-09-04 (저녁)
- [함정·중요] `cedar_post.sh` 로 18:26 매일 괴담 루틴과 동시에 편집기를 열었다가 충돌.
  예약 달력 클릭이 실패했는데도 스크립트가 그대로 `발행` 을 눌러 **"지금 공개발행" 이 될 뻔했다**
  (다행히 저장 자체가 실패해 글은 안 생겼고, 그날 괴담 2편도 무사).
  → 스크립트에 3중 안전장치 추가: ① 18:20~18:55 실행 거부(`CEDAR_FORCE=1` 로만 우회)
  ② `/tmp/tistory-editor.lock` 디렉터리 잠금(20분 지나면 죽은 잠금으로 보고 회수)
  ③ 발행 직전 **날짜·시·분이 요청값과 정확히 같을 때만** 발행 버튼 클릭.
- [연재] 왼쪽/오른쪽 게임 Part 4 → 4-1/4-2/4-3 으로 나눠 9/27~9/29 21:00 예약.
- [함정] 예약 달력은 **이번 달만 보여준다**. 다음 달 날짜를 누르려면 `.box_calendar .btn_next` 로 넘겨야 하고,
  안 넘기면 이번 달 같은 날짜(과거·disabled)를 눌러 실패한다. `.txt_calendar` 텍스트("2026년 9월")로 확인.
  disabled 버튼은 제외하고 찾을 것.
- [연재] 왼쪽/오른쪽 게임 Part 5 → 5-1~5-4 로 나눠 9/30~10/3 21:00 예약. (아폴로·이브 사망 편)
- [함정·중요] `cedar_post.sh` 에 **날짜를 숫자로만** 넘기면 "이번 달 그 날"로 해석돼서,
  오늘과 같은 날짜(4)를 넘겼더니 **오늘 21시 공개 예약**이 돼버렸다. 이제 `YYYY-MM-DD` 를 받는다. 그렇게 쓸 것.
- [도구] `scripts/cedar_reschedule.sh <글번호> <YYYY-MM-DD> [시] [분]` — 이미 올린 예약글의 일시만 고친다.
  편집 URL `/manage/post/<id>` 는 `/manage/newpost/<id>?type=post...` 로 리다이렉트되므로 탭 매칭은 newpost 로.
- [사고] 매일 괴담 루틴과 겹친 그 실패한 발행이 사실은 저장돼 있었다 —
  제목 `…4-221`, 본문은 두 줄 괴담, 카테고리 두 줄 괴담, **9/4 19:32 공개 예약**. 발견해서 삭제(/185).
  같은 충돌이 나면 반드시 `?searchKeyword=` 로 검색해 잔재를 확인할 것. 목록 첫 페이지만 보면 놓친다.
- [연재] 왼쪽/오른쪽 게임 Part 6 → 6-1~6-4, 10/4~10/7 21:00 예약. (보니·클라이드 퇴장)
- [삽화] 예약글 33편에 붙일 삽화를 codex-image 로 생성(`scripts/illust_gen.sh <글번호> "<장면>"`).
  화풍은 세 시리즈 통일: 어두운 사진풍 실사 + 35mm 그레인 + 탈색 청록/회색, 사람·글자·피 없음.
  소재만 시리즈별로 — 시더빌=병원 내부, 뿔=숲/시골, 게임=도로/차. 파일명은 글 제목(`/`→`-`, `?` 제거).
- [미해결] 삽화를 본문에 넣는 경로. 편집기 첨부 입력은 `#attach-layer-btn` 을 눌러야 생기는
  `#attach-image`(accept=image/*)인데, `aside.sh file` 로 파일을 물리고 change 를 쏴도
  "업로드 중"에서 멈추고 본문에 img 가 안 들어온다. 합성 이벤트로는 업로드가 완결되지 않는 듯.
- [함정·중요] 티스토리에는 **하루 작성 글 수 제한**이 있다. 2026-09-04 하루에 게임 4-1~7-1 + 매일 괴담 2편 +
  재발행 여러 건을 몰아치다 한도에 걸렸다. 걸린 뒤에는 발행을 눌러도 **글이 아예 안 생긴다.**
  **한도는 정확히 하루 50편**(블로그 전체 기준). 9/3 마지막 글이 147번이고 오늘 148~197번까지 만들어진 뒤
  198번이 안 만들어졌다. 이 50편은 **블로그를 쓰는 모든 세션이 나눠 쓴다** —
  오늘은 내 리마스터 33 + 여행 글 세션 14 + 매일 괴담 2 + 충돌 잔재 1 = 50.
  → "하루 3~4편"은 지나치게 보수적이었다. 실제로는 하루 30편 넘게 올려도 되지만,
  다른 세션 몫(여행 글·괴담 루틴)을 남겨둘 것. 여행 글 세션과 같은 날 몰아치면 서로 한도를 잡아먹는다.
- [버그] `cedar_post.sh` 는 발행 버튼을 누른 뒤 **결과를 확인하지 않고** "✓ 발행 요청 완료"를 찍는다.
  한도에 걸린 7-1도 성공으로 찍혔지만 실제로는 글이 없었다. → 발행 후 목록에서 제목 검색으로 확인하는 단계를 넣을 것.
- [가설·미확인] 삽화 업로드가 "업로드 중"에서 멈춘 것도 같은 하루 한도 때문일 수 있다(첨부도 한도에 포함되는 듯).
  내일 한도가 풀린 뒤 `#attach-image` 경로를 **먼저 다시 시험**해볼 것. 합성 이벤트 문제로 단정하지 말 것.
- [연재] Part 7 번역 4편(7-1~7-4) 완료. 빌드까지 됐고 **예약은 못 했다** — 내일 10/8~10/11로 올릴 것.

## 2026-09-05
- [HQ 재설계] 파이프라인 HQ를 읽기 전용 지표 대시보드로 축소 — 발행 버튼·시간표 편집·괴담 생성 탭 전부 제거(사용자 지시). 플랫폼별 자동발행시간(read-only)·조회수·수익·방문자만 표시. 데이터 함수(dashboard.py)는 그대로 재사용. 자동발행은 각 저장소 GitHub Actions/예약작업이 담당.
- [함정] 인스타·틱톡·네이버클립은 공개 조회수 API가 없어 스냅샷(space-shorts channel-stats.json platforms). 유튜브만 실측.

## 2026-09-05
- [함정·해결] **Aside 는 WebKit 계열이라 네이티브 confirm 을 CDP 로 못 닫는다.**
  새 글쓰기를 열 때 뜨는 "…에 저장된 글이 있습니다. 이어서 작성하시겠습니까?" 가 렌더러를 막아
  그 탭의 `eval`·`shot` 이 전부 타임아웃난다. `dismiss` 는 Aside 에선 **항상 False** 라 판별에 못 쓴다.
  → `~/.claude/tools/screen/aside_confirm.sh cancel` (화면 잠금 + screencapture 좌표 + click.py) 로 물리 클릭.
  `cedar_post.sh` 가 편집기 무응답이면 자동으로 이걸 부르고 재시도한다.
- [함정·중요] **발행 확인을 `?searchKeyword=` 로 하면 안 된다.** 방금 만든 글이 검색에 안 잡혀
  "발행 실패"로 오판했다. 실제로는 멀쩡히 만들어져 있었다. **`/manage/posts/` 목록 첫 페이지를 직접 볼 것.**
  (어제 "한도 때문에 발행이 안 된다"고 본 것도 이 오판이 섞여 있었을 수 있다)
- [함정] 렌더러가 막히면 `aside.sh goto <탭> about:blank` 로 빠져나온다(브라우저 프로세스가 처리해서 통함).
- [함정] zsh 는 unquoted 변수를 word-split 하지 않는다 — `for a in "x 1" "y 2"; do set -- $a` 가 안 먹는다.
  (이미 적어뒀는데 또 밟았다. 반복문 대신 명시적으로 한 줄씩 쓸 것)
- [문서] Aside 조작 매뉴얼을 `~/.claude/rules/aside-browser.md` 에 만들고 전역 CLAUDE.md 에 import.
  **모든 세션이 자동으로 읽는다.** 새로 알아낸 함정은 거기에 계속 추가할 것.
- [원인규명·중요] 8-1~8-3 발행 실패의 진짜 원인은 **CAPTCHA(DKAPTCHA)** 였다.
  발행 레이어 위에 "지도에서 장소를 찾아 빈칸을 채우세요" 문제가 뜨고 "저장중"에서 멈춘다.
  화면을 안 보면 그냥 발행 실패로만 보인다. 하루 한도(50편)와는 **별개** — 오늘 5편째부터 걸렸다.
  **CAPTCHA 는 자동으로 풀지 않는다(봇 감지 우회 금지). 사람이 풀어야 한다.**
  → 앞으로는 한 번에 몰아치지 말고 편수를 나누고 간격을 둘 것.
- [함정] 목록 탭이 여러 개 쌓이면 `find()` 가 좀비 탭을 잡아 **검증이 항상 false** 가 된다.
  실제로 만들어진 8-4 도 "없음"으로 읽었다. 읽어서 판단하기 전엔 `closeend` 로 정리하고 새로 열 것.
  `cedar_post.sh` 검증부에 반영했다.
- [연재] Part 8 번역 4편 완료. **8-4만 예약됨(10/15)**, 8-1~8-3 은 빌드까지 끝났고 발행만 남음.
- [해결·중요] **삽화를 본문에 넣는 방법을 찾았다** (여행 블로그 세션에서 배움).
  `#attach-layer-btn` → `#attach-image` 경로는 파일만 물리고 업로드가 안 걸린다.
  **툴바 "첨부" 버튼 → 드롭다운 "사진" 항목**을 클릭해서 생기는 input 이라야 업로드→본문 삽입까지 된다.
  선택자는 `input[type=file][accept^="image"]`(편집 페이지는 accept 가 목록형이라 ^= 필요).
  완료 확인은 `figure[data-ke-type=image] img[src*=kakaocdn]` 개수 폴링. 상세는 ~/.claude/rules/aside-browser.md.
  → 159 에 실제로 삽입 성공. `H2 → FIGURE(img) → P` 로 배치까지 확인. **저장만 CAPTCHA 에 막힘.**
- [함정] **기존 글 수정에도 CAPTCHA 가 뜬다.** 새 글 작성만 막히는 게 아니다.
- [연재] **왼쪽/오른쪽 게임 Part 10 번역 완료 — 시리즈 완결.** 10-1~10-4, 빌드까지 끝. 발행만 남음.
  (원작자와 현재도 협업 중이고 블로그 연재 후 유튜브 영상까지 함께 만들 예정이라고 확인받음)
- [삽화] 총 49장. 예약글 33편에는 삽입·검증 완료. 나머지 16장(Part 7·8·9·10)은 생성만 해둔 상태.
- [함정] **오늘 만든 글은 같은 날 수정도 안 된다.** 198~202(Part 7·8-4)에 삽화를 넣으려 했으나
  다섯 편 다 저장 실패(이미지 0). 어제 만든 33편 수정은 됐다. 하루 한도가 새 글 작성뿐 아니라
  당일 생성분 저장 전체에 걸리는 것으로 보인다. → 삽화는 글을 올린 **다음 날** 넣을 것.
- [상태] 예약 38편(9/5~10/15). **빈칸 10/12~10/14** = 8-1~8-3 자리. 내일 채울 것.
  발행 대기: 8-1~8-3, 9-1~9-4, 10-1~10-4 (전부 빌드 완료). 글번호 매핑은 state/postmap.txt.

## 2026-09-06
- [카페홍보] `scripts/cafe_promo.sh <cedar파일>` 신설 — 티스토리 글을 다음카페 여시
  (subdued20club / fldid=RaxJ 「홍콩할매의 속삭임」)에 홍보 게시. 첫 실행으로 시더빌 1편 → 115174번
- [형식] 사용자 기존 글에서 확인: 말머리=**소설**(단편·두줄·시리즈 전부 동일), 제목=티스토리 제목 그대로,
  본문 = 카페 기본 배너 + `출처 : <티스토리링크>` + 링크미리보기카드 + 본문 전문.
  등록설정은 **스크랩 허용 O / 복사 허용 X**
- [함정] 글쓰기 편집기 본문에 **"출처 : " 가 이미 들어있는 템플릿**이 깔려 있다. 그대로 또 치면
  "출처 : 출처 : " 가 된다 → 엔터 후 정규식으로 중복 제거할 것
- [함정] 카페 글쓰기 진입 URL 이 없다(`_write`·`bbs_write` 전부 404). 게시판 열고
  bbs_list 프레임의 **`#article-write-btn` 을 클릭**해야 한다
- [함정] 편집기가 열리면 **탭 URL 이 `/subdued20club` 로 바뀐다**(RaxJ 가 사라짐).
  탭 정규식을 `/RaxJ$/` 로 잡아두면 못 찾는다. 단계마다 정규식을 바꿔 잡을 것
- [함정] `~/RaxJ$` 는 404 페이지 `_memo/RaxJ` 까지 잡는다 → `subdued20club/RaxJ$` 로 고정
- [함정] `aside repl` 의 `openTab` 으로 연 탭은 **repl 종료 시 닫힌다.** 여러 호출에 걸쳐 쓸 탭은
  `aside.sh open` 으로 열고 repl 은 `attachBrowserTab` 으로 붙일 것
- [편집기] 카페도 TinyMCE(`#keditorContainer_ifr`). 제목은 진짜 키보드 입력,
  본문은 합성 `ClipboardEvent('paste')` — 티스토리와 동일한 규칙이 그대로 적용된다
- [함정·여시스타일] 카페 본문은 **원고의 줄 하나 = `<p>` 하나, 그 사이마다 빈 `<p>&nbsp;</p>`**.
  빈 줄로 나뉜 덩어리만 문단으로 잡고 내부 개행을 `<br>` 로 이으면 그 자리만 줄이 붙는다
  (첫 게시 115174 에서 사용자가 직접 고쳐줌). 문단 전환의 추가 빈 줄은 넣지 않아도 된다.
- [카페홍보] `cafe_promo.sh` 에 **글번호 모드** 추가 — `cafe_promo.sh 145` 처럼 티스토리 글번호만 주면
  발행글에서 제목·본문을 그대로 긁어온다(원고 파일·postmap 불필요). 두 줄 괴담 551~560 → 115175
  · 본문 뒤의 **「해설」(두 번째 H2) 섹션부터는 잘라낸다** — 블로그용 요약이라 카페엔 안 넣는다
  · 원문 링크·작성자(u/…) 줄은 그대로 살린다
- [함정] bash 큰따옴표 안에서 JS 정규식 끝을 `\\$` 로 쓰면 **리터럴 `$` 가 들어가** 탭을 못 찾는다.
  `\$` 로 쓰거나, 아예 `x.url.replace(/[?#].*/,'').endsWith('/145')` 처럼 정규식을 피할 것
- [함정] 발행 확인의 `완료화면` 체크(post_article/bbs_done 프레임)는 false 가 나와도 글은 올라가 있다.
  **목록 첫 페이지를 직접 읽는 8단계 확인이 유일하게 믿을 만하다**
- [정책] **해설 섹션은 카페에 절대 안 넣는다**(사용자 확정 2026-09-06). 블로그 글마다 형태가 다르다:
  두 번째 `H2` 로 오기도 하고, 본문 안에 `[해설]` 한 줄(`<p>`)로 오기도 한다.
  스크립트는 둘 다 잡아 `원문 링크:` 전까지 버린다. **원문 링크·작성자(u/…) 줄은 남긴다.**
- [함정] 카페 글의 「수정」 버튼을 innerText 로만 찾으면 **회원정보 수정(`a.modify_profile` → `join_edit`)**
  이 먼저 잡힌다. 글 수정은 `.area_r` 안의 **보이는 `button.btn_g`**(`offsetParent!==null`) 이다
- [함정] 프레임 이름이 화면마다 다르다 — 읽기 `bbs_read` **또는** `bbs_nsread`,
  새 글 `united_write`, **글 수정 `bbs_rewrite`**. 정규식을 `united_write|bbs_rewrite`, `bbs_n?s?read\?` 로 둘 것

## 2026-09-07
- [보안] GitHub `hyjh1006-afk/tistory-cloud` 를 **PUBLIC → PRIVATE** 전환(기획조정실이 전달한 사용자 승인).
  README 상 원래 Private 으로 만들게 되어 있던 레포라 설계대로 되돌린 것.
  비밀값 유출은 없었다 — `.gitignore` 가 `DEPLOY_SECRETS.txt`·`stage2_token.json`·`.streamlit/secrets.toml` 을 막고 있었다.
- [함정] 이 레포는 **`gwidam-maker.streamlit.app`(파이프라인 HQ)의 소스**다. 공개여부를 건드릴 땐 Pages 뿐 아니라
  **Streamlit 배포가 깨지는지**를 반드시 같이 본다. 그런데 이 앱은 평상시에도 로그인 게이트라
  **`curl -sI` 상태코드만으론 죽었는지 살았는지 구분이 안 된다.**
  → 전환 **전에 기준선을 재두고 전후 응답을 대조**할 것. 기준선 = `303` + `location: https://share.streamlit.io/-/auth/app?redirect_uri=...`
  전환 후 동일함을 확인했다(로그인 뒤 화면은 확인 불가 — 사람 몫).
- [롤백] 되돌리려면 `gh repo edit hyjh1006-afk/tistory-cloud --visibility public --accept-visibility-change-consequences`
- [정정·중요] 위 전환은 **롤백했다**(같은 날). 사용자가 파이프라인 HQ 를 직접 열어보니 Streamlit "Oh no" 에러.
  private 전환으로 Streamlit Community Cloud 가 레포 접근을 잃은 것. `--visibility public` 으로 되돌려 복구.
- [교훈] **`303` 기준선 대조로 "이상 없음"이라고 결론낸 것은 틀렸다.** 전환 후 응답이 기준선과
  상태코드·리다이렉트까지 완전히 같았는데도 **앱은 이미 깨져 있었다.** 에러 화면이 로그인 게이트 뒤에 있어서다.
  → 로그인 뒤에 있는 앱은 **바깥에서 어떤 HTTP 확인으로도 생사를 판정할 수 없다.** 사람이 열어보는 것만이 유일한 검증이다.
  자동 확인으로 할 수 있는 건 "익명으로 레포가 읽히는가"(`raw.githubusercontent.com` 200) 같은 **전제조건 확인**까지다.
- [남은 것] private 유지가 필요하면 Streamlit Cloud 에서 GitHub 접근 재승인 후 앱 Reboot 가 필요하다(OAuth 권한 부여라 사람 몫).
- [복구] 롤백만으론 부족했다. Streamlit 이 clone 실패 상태에 **갇혀 있어 자동 복구가 안 된다** —
  레포를 public 으로 되돌린 뒤 **앱 Reboot 까지** 해야 산다.
  절차: 앱 페이지 우하단 **`Manage app`** 클릭(그 버튼은 `position:fixed` 라 `offsetParent` 가 null 이니
  가시성 필터로 거르면 못 찾는다) → 패널 하단 **`button._button_clfh0_1`**(⋮) → **`Reboot app`** → 확인 모달의 `Reboot`.
  ⚠️ 메뉴에 **`Delete app` 이 `Reboot app` 바로 아래** 있다. 좌표·인덱스로 누르지 말고 **정확한 텍스트 일치**로만 클릭할 것.
  패널은 iframe 이 아니라 메인 문서에 그려진다(`p.evaluate` 로 닿는다).
  복구 확인은 로그에서 `🐙 Cloned repository!` → `📦 Processed dependencies!` → `Uvicorn server started` 를 본다.
- [원인규명] 삽화가 어제부터 안 붙던 진짜 원인은 **DKAPTCHA** 였다. 그런데 스크립트가 캡챠를 **두 겹으로** 못 봤다:
  ① `"정답을 입력해주세요"` 는 input 의 **placeholder 속성**이라 `innerText` 에 안 잡힌다
  ② DKAPTCHA 자체가 **iframe 안**에 그려져서 최상위 document 만 보면 아예 안 보인다
  → `scripts/captcha_check.sh <글번호>` 신설. **모든 프레임을 순회**해 판정한다(y/n/notab). 실측으로 잘 잡힌다.
- [함정] `illust_insert.sh` 의 **exit code 를 믿지 말 것.** 204·205·207 전부 exit 1 이었는데 실제로는 저장돼 있었다
  (편집기 탭이 닫힌 뒤 확인해서 나는 오탐). **확인은 발행글을 직접 열어 `img[src*=kakaocdn]` 로** 한다.
- [기준] 삽화 배치 정상값은 발행글 기준 **`H2>P>FIGURE>P`**(제목 → 원출처 → 그림). 198~207 대조로 확정.
  편집기 안에서는 `H2>FIGURE>P` 로 보이는 게 맞다(발행 시 원출처 P 가 사이에 들어간다).
- [신규] `scripts/illust_fix_pos.sh <글번호>` — 이미 삽화가 있는 글의 **위치만** 제목 밑으로 옮겨 재저장한다.
  `illust_insert.sh` 는 이미지가 있으면 건너뛰므로 위치 교정에는 못 쓴다.
- [남은 일 2026-09-07] **203 만 미완**(발행글 `P>FIGURE>P>H2` — 그림이 제목 위). 캡챠를 못 풀어 저장 실패.
  재개: `bash scripts/illust_fix_pos.sh 203` → 캡챠 뜨면 사람이 풀기 → 발행글에서 `H2>P>FIGURE` 확인.
  204·205·206·207 은 완료. 그다음 연재 6편(9-3·9-4·10-1~10-4) 발행이 남아 있다.
- [함정·중요] **매일 게시 확인을 「/manage/posts/ 첫 페이지 읽기」로 하면 안 된다.** 예약글이 발행일
  내림차순으로 앞을 다 차지해서(현재 예약 15편이 1페이지를 꽉 채움) 오늘 저장한 글이 1페이지에 아예 안 뜬다.
  → **`/manage/posts/?visibility=hidden`(비공개 필터)로 확인**할 것. 오늘 비공개 저장분만 걸러져 한눈에 보인다.
  글번호는 그 행의 `a[href^="/manage/post/<번호>"]` 에서 딴다.
- [함정] 그 목록을 `fetch()` + 문자열 매칭으로 확인하면 **멀쩡히 있는 글도 "없음"으로 나온다**
  (실측: 방금 저장한 208 이 `t.includes("571~580") === false`). 목록은 JS 렌더라 응답 HTML 에 제목이 없다.
  **반드시 탭에서 DOM(`querySelectorAll("li,tr").innerText`)으로 읽을 것.** 발행글 본문 확인은 fetch 로 잘 된다.
- [캡챠] 한 세션에서 **두 번째 글을 저장할 때** DKAPTCHA 가 떴다(1편 208 저장 성공 → 2편에서 발생).
  증상: `비공개 저장` 클릭 후 페이지가 안 넘어가고 버튼이 **"저장중"** 에서 멈춘다.
  판별: `document.querySelector("iframe[src*=dkaptcha]")` 가 잡힌다(최상위 문서에 있어 바로 보인다).
  캡챠는 자동으로 풀지 않는다 — 편집기 탭에 원고를 그대로 둔 채 사람에게 넘긴다. 대기 목록에서도 지우지 않는다.
